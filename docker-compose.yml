services:
  mysql:
    image: mysql:8.4
    command:
      - --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-mentorly}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_USER: ${MYSQL_USER:-mentorly}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-u", "root", "-p$${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-prod}
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:-mentorly}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-mentorly}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-mentorly}
      JWT_SECRET: ${JWT_SECRET}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:5174,http://127.0.0.1:5174}
      # Admin bootstrap — AdminDataInitializer seeds a single admin on startup
      # when these are set. Without them a fresh deployment has no admin user
      # and the admin module is unreachable.
      APP_ADMIN_EMAIL: ${APP_ADMIN_EMAIL:-}
      APP_ADMIN_PASSWORD: ${APP_ADMIN_PASSWORD:-}
      APP_ADMIN_NAME: ${APP_ADMIN_NAME:-Platform Admin}
      # OAuth2 callback — application-prod.yml overrides the base default with
      # ${OAUTH2_REDIRECT_URL} (no fallback), so it must be provided in prod.
      OAUTH2_REDIRECT_URL: ${OAUTH2_REDIRECT_URL:-http://localhost:5174/oauth/callback}
      APP_RATE_LIMIT_AUTH_PER_MINUTE: ${APP_RATE_LIMIT_AUTH_PER_MINUTE:-20}
      APP_RATE_LIMIT_PAYMENT_PER_MINUTE: ${APP_RATE_LIMIT_PAYMENT_PER_MINUTE:-30}
      # Payment gateway credentials (NEVER expose to frontend)
      APP_PAYMENT_RAZORPAY_KEY_ID: ${APP_PAYMENT_RAZORPAY_KEY_ID:-}
      APP_PAYMENT_RAZORPAY_KEY_SECRET: ${APP_PAYMENT_RAZORPAY_KEY_SECRET:-}
      APP_PAYMENT_RAZORPAY_FAIL_ON_PLACEHOLDER: ${APP_PAYMENT_RAZORPAY_FAIL_ON_PLACEHOLDER:-false}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:-}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET:-}
      STRIPE_CONNECT_WEBHOOK_SECRET: ${STRIPE_CONNECT_WEBHOOK_SECRET:-}
    ports:
      - "8080:8080"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Same-origin: nginx proxies /api, /oauth2, /login/oauth2 and /ws to
        # backend:8080, so browsers only ever talk to the frontend host.
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
        VITE_APP_ENV: ${VITE_APP_ENV:-production}
        # Public payment keys (safe to expose in browser bundle)
        VITE_RAZORPAY_KEY_ID: ${VITE_RAZORPAY_KEY_ID:-}
        VITE_STRIPE_PUBLISHABLE_KEY: ${VITE_STRIPE_PUBLISHABLE_KEY:-}
    depends_on:
      - backend
    ports:
      - "5174:80"
      - "${FRONTEND_HTTPS_PORT:-443}:443"
    volumes:
      # Mount SSL certificates. RW so docker-entrypoint.sh can bootstrap a
      # temporary self-signed cert on first boot before Let's Encrypt
      # provisioning (nginx refuses to start without cert files).
      # scripts/setup-ssl.sh later replaces these with real certificates.
      - ${SSL_CERT_DIR:-./certs}:/etc/nginx/ssl
      # Mount certbot challenge directory for initial certificate issuance
      - certbot_www:/var/www/certbot
    restart: unless-stopped

  # ── Certbot (Let's Encrypt) — for automatic SSL certificate management ──
  # Usage:
  #   docker compose --profile ssl run certbot certonly --webroot \
  #     -w /var/www/certbot -d yourdomain.com
  # Renewal (set up as daily cron):
  #   docker compose --profile ssl run certbot renew && \
  #     docker compose exec frontend nginx -s reload
  certbot:
    image: certbot/certbot:latest
    volumes:
      - certbot_conf:/etc/letsencrypt
      - certbot_www:/var/www/certbot
      - ${SSL_CERT_DIR:-./certs}:/certs
    profiles:
      - ssl  # Only run explicitly, not on `docker compose up`
    command: --help

volumes:
  mysql_data:
  certbot_conf:
  certbot_www:
  certs: